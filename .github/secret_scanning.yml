# The moderation cases and tests contain fake credentials on purpose: Blackline has to
# recognise the shape of Slack, AWS, GitHub, Google, and OpenAI keys. None of them are real.
# Excluding these paths keeps secret scanning and push protection useful everywhere else.
paths-ignore:
  - "evals/cases.yaml"
  - "tests/**"
